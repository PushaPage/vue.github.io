import { createStore } from 'vuex';
import users from './modules/users-module';

export default createStore({
    state: {},
    mutations: {},
    actions: {},
    modules: {
        users,
    },
});
